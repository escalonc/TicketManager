<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cejteam.ticketmanager">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_logoopcion"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_logoopcion"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.Dark">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
			<intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CrearEventoDeportivo">
        </activity>
        <activity android:name=".MenuEvents">
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".SignUpActivity"
            android:label="@string/action_sign_in"
            android:theme="@style/AppTheme.Dark" />
        <activity
            android:name=".UserListActivity"
            android:label="@string/title_activity_user_list"
            android:theme="@style/AppTheme"></activity>
        <activity android:name=".EliminarEvento" />
        <activity android:name=".VerEvento" />
        <activity android:name=".InformacionDeleEvento" />
        <activity android:name=".CrearEventoMusical" />
        <activity android:name=".CrearEventoReligioso" />
        <activity android:name=".TipodeEvento" />
        <activity android:name=".TipoDeEventoEditar" />
        <activity android:name=".EventosRealizadosActivity" />
        <activity android:name=".EventosFuturosActivity" />
        <activity android:name=".Reports" />
        <activity android:name=".EventosEntreDosFechas">
        </activity>
        <activity android:name=".EventosCancelados" />
        <activity android:name=".FechaInicial" />
        <activity android:name=".FechaFinal" />
        <activity android:name=".ListaJugadoresPorEquipo" />
        <activity android:name=".SeleccionarTeam"></activity>
    </application>

</manifest>